import React, { PureComponent } from "react";
import PageDefault from "pages/PageDefault";

export default class ByTeamForMgmt extends PureComponent {
  render() {
    return <PageDefault {...this.props} />;
  }
}
